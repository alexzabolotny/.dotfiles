alias la="ls -la"
alias vup="vagrant up"
alias vdn="vagrant halt"

#alias portsup="sudo pfctl -ef /etc/pf.conf"
#alias portsdn="sudo pfctl -df /etc/pf.conf"

alias pu="./vendor/bin/phpunit"

# PHP Laravel
alias a="php artisan"
alias t="php artisan test"
alias tf="php artisan test --filter"
